# Configure the prompt
[character]
success_symbol = '[➜](bold green)'
error_symbol   = '[✕](bold red)'

[directory]
read_only = "(read only)"

[git_commit]
tag_disabled = false
only_detached = false

[git_status]
disabled = false

[hostname]
ssh_symbol = "ssh@"
